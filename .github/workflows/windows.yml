#  SPDX-License-Identifier: GPL-2.0-or-later

name: "Windows builds"
on: push
jobs:

  build_msvc_debug:
    name: "MSVC 14 debug (win-2019)"
    runs-on: windows-2019
    steps:
      - uses: actions/checkout@v1
      - name: "Install packages"
        shell: pwsh
        run: |
          vcpkg install libpng sdl1 sdl1-net opusfile speexdsp
          vcpkg integrate install
      - name: "Build"
        shell: pwsh
        env:
          PATH: '${env:PATH};C:\Program Files (x86)\Microsoft Visual Studio\2019\Enterprise\MSBuild\Current\Bin\amd64'
        run: |
          # build steps
          cd visualc_net
          MSBuild -m dosbox.sln -p:Configuration=Debug

  build_msvc_release:
    name: "MSVC 14 optimized (win-2019)"
    runs-on: windows-2019
    steps:
      - uses: actions/checkout@v1
      - name: "Install packages"
        shell: pwsh
        run: |
          vcpkg install libpng sdl1 sdl1-net opusfile speexdsp
          vcpkg integrate install
      - name: "Build"
        shell: pwsh
        env:
          PATH: '${env:PATH};C:\Program Files (x86)\Microsoft Visual Studio\2019\Enterprise\MSBuild\Current\Bin\amd64'
        run: |
          # build steps
          cd visualc_net
          MSBuild -m dosbox.sln -p:Configuration=Release

  build_msys2_gcc_64:
    name: MSYS2 GCC-9.2 x86_64
    runs-on: windows-2019
    steps:
      - uses: actions/checkout@v1
      - name: Install msys2 environment
        run: choco install msys2 --no-progress
      - name: Install dependencies
      - run: C:\tools\msys64\usr\bin\bash -lc "pacman -S --noconfirm $($GITHUB_WORKSPACE/scripts/build.sh --list-packages --bin-path /mingw64/bin)"
      - name: Build
        run: C:\tools\msys64\usr\bin\bash -lc "$GITHUB_WORKSPACE/scripts/build.sh --bin-path /mingw64/bin --src-path $GITHUB_WORKSPACE"
      - name: Summarize warnings
        run: C:\tools\msys64\usr\bin\python3 ./scripts/count-warnings.py build.log

  build_msys2_gcc_32_small:
    name: MSYS2 GCC-9.2 small i686
    runs-on: windows-2019
    steps:
      - uses: actions/checkout@v1
      - name: Install msys2 environment
        run: choco install msys2 --no-progress
      - name: Install dependencies
      - run: C:\tools\msys64\usr\bin\bash -lc "pacman -S --noconfirm $($GITHUB_WORKSPACE/scripts/build.sh --list-packages --bit-depth 32 --bin-path /mingw32/bin)"
      - name: Build
        run: C:\tools\msys64\usr\bin\bash -lc "$GITHUB_WORKSPACE/scripts/build.sh --bit-depth 32 --bin-path /mingw32/bin --src-path $GITHUB_WORKSPACE --release small"
      - name: Summarize warnings
        run: C:\tools\msys64\usr\bin\python3 ./scripts/count-warnings.py build.log

  build_msys2_clang_64_debug:
    name: MSYS2 Clang-8.0.1 debug x86_64
    runs-on: windows-2019
    steps:
      - uses: actions/checkout@v1
      - name: Install msys2 environment
        run: choco install msys2 --no-progress
      - name: Install dependencies
      - run: C:\tools\msys64\usr\bin\bash -lc "pacman -S --noconfirm $($GITHUB_WORKSPACE/scripts/build.sh --list-packages --compiler clang --bin-path /mingw64/bin)"
      - name: Build
        run: C:\tools\msys64\usr\bin\bash -lc "$GITHUB_WORKSPACE/scripts/build.sh --compiler clang --bin-path /mingw64/bin --src-path $GITHUB_WORKSPACE --release debug"
      - name: Summarize warnings
        run: C:\tools\msys64\usr\bin\python3 ./scripts/count-warnings.py build.log

  build_msys2_clang_32_small:
    name: MSYS2 Clang-8.0.1 small i686
    runs-on: windows-2019
    steps:
      - uses: actions/checkout@v1
      - name: Install msys2 environment
        run: choco install msys2 --no-progress
      - name: Install dependencies
      - run: C:\tools\msys64\usr\bin\bash -lc "pacman -S --noconfirm $($GITHUB_WORKSPACE/scripts/build.sh --list-packages --bit-depth 32 --compiler clang --bin-path /mingw32/bin)"
      - name: Build
        run: C:\tools\msys64\usr\bin\bash -lc "$GITHUB_WORKSPACE/scripts/build.sh --bit-depth 32 --compiler clang --bin-path /mingw32/bin --src-path $GITHUB_WORKSPACE --release small"
      - name: Summarize warnings
        run: C:\tools\msys64\usr\bin\python3 ./scripts/count-warnings.py build.log
