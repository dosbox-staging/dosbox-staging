if (APPLE)
  add_compile_options("-Wno-cast-function-type-mismatch")
endif()

if (CMAKE_BUILD_TYPE STREQUAL "Debug")
  if (C_ANGLE)
  add_library(libglad STATIC debug/src/gles2.c)
  else()
  add_library(libglad STATIC debug/src/gl.c)
  endif()
  target_include_directories(libglad PUBLIC debug/include)
else() # Release
  if (C_ANGLE)
  add_library(libglad STATIC release/src/gles2.c)
  else()
  add_library(libglad STATIC release/src/gl.c)
  endif()
  target_include_directories(libglad PUBLIC release/include)
endif()
